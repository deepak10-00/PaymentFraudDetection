[pytest]
# Add the project root to the Python path to allow imports from 'app'
pythonpath = .

# Define environment variables for the test suite
# This ensures tests run with a consistent and isolated configuration
env =
    FASTAPI_ENV=testing
    DEBUG=True
    # Use a separate test database if needed
    # MYSQL_DB=test_fraud_detection_db 
    # MONGO_DB=test_honeypot_db
